# Dependencies
node_modules/
.pnpm-store/
*.log

# Build outputs
dist/
build/
.astro/
.cache/
.next/

# Environment / secrets
.env
.env.local
.env.*.local
.dev.vars

# Vercel
.vercel/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Bootstrap inputs (never commit raw Twitter export)
apps/bootstrap/input/

# iOS Shortcut binaries — they bake CAPTURE_TOKEN_IOS into the plist.
# The generator (ios/build_shortcut.py) is in-tree; the output is not.
ios/*.shortcut
